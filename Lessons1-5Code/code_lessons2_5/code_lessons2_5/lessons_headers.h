#pragma once // include guard to prevent being inserted multiple times in a cpp file
#include <iostream>

#ifndef LESSONS_HEADERS.H // include guard to prevent being inserted multiple times in a cpp file
#define LESSONS_HEADERS.H

namespace nmsp {
	int test(int x);
}

#endif